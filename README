# MUI PR deployment opener

This extension opens a new tab and navigates to a deployment assocated with the currently viewed PR.

## Installation

1. Clone the source code
2. Go to your browser extensions page (chrome://extensions/ or edge://extensions/)
3. Enable the developer mode to be able to install local extensions
4. Click Load unpacked and choose the cloned repo directory

## Usage

To use it, have a PR open (either in github.com or github.dev) and click on the extension button.
A new tab will open with its URL set to `https://deploy-preview-[PR_number]--material-ui.netlify.app`
